# reporting/

Job manifest and human-readable processing report generation
(architecture.md §4.4, §15.3): the JSON job manifest (app version, engine
identity, inputs, options, output paths, timestamps, exit code,
warnings/errors) and the plain-language report shown on the Results screen.

Empty in Phase 0.
