# features/settings/

Application settings UI (architecture.md §15.1): theme, default output
directory, default conflict policy, recent-files/history preferences,
advanced-command visibility, update-check preference (opt-in only).

Not implemented yet - part of Phase 6 (Persistence and release quality).
