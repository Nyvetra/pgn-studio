# features/filters/

Filters screen (architecture.md §13.4): player, result, date/year, Elo, ECO,
move-count, checkmate-only, and SetUp/FEN filters. The backend converts UI
filter selections into generated criteria files - filter syntax must never
be composed in React.

Not implemented yet - part of Phase 5 (Filters).
