# features/review/

Review screen (architecture.md §13.5): operation summary, ordered sources,
destination artifacts, conflict behavior, warnings, engine identity, and the
optional generated (display-only, never executed) command preview.

Not implemented yet - part of Phase 2 (Core workflow UI).
