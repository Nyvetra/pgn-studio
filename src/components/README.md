# components/

Shared, presentational UI components with no feature-specific business logic
(buttons, form fields, layout primitives, etc.).

Feature-specific components belong under `src/features/<feature>/` instead.
This directory is empty in Phase 0 - it is created now so later phases have
an obvious, agreed home for shared UI rather than inventing the convention
under time pressure.
